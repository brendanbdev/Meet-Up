package com.example.thirdtry

import androidx.lifecycle.ViewModel

class HardcoreViewModel : ViewModel() {
    //private val containing a "List" and calling .toMutableStateList() on that "list"
    //public val getting private val (example: val tasks: List<WellnessTask> get() = _tasks)
    //all list data, getter and setter functions

    //ViewModels provide the UI state and access to the business logic located in other layers of the app.
}